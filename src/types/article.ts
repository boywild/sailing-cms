export interface Category {
  id: string
  title: string
  type: string
}

export interface Article {
  articleId: string
  title: string
  image: string
  content: string
  createDate: string
  author: string
}
